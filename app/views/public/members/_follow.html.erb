<% if !current_member.following?(member) %>
  <%= link_to "フォローする", member_follows_path(member), method: :post, remote: true, class:"btn btn-primary btn-lg" %>
<% else %>
  <%= link_to "フォロー外す", member_follows_path(member), method: :delete, remote: true, class:"btn btn-warning btn-lg" %>
<% end %>